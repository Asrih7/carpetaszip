import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aulavirtualctp.armentapenitenciario',
  appName: 'armentapenitenciario',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
